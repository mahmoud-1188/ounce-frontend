const sanitizeText = (v, max = 300) =>
  typeof v === "string"
    ? v.replace(/[\u0000-\u001F\u007F]/g, "").replace(/\s+/g, " ").trim().slice(0, max)
    : v;


// ═══════════════════════════════════════════════════════════════════════
//  وحدة استرجاع المبيعات
//
//  المنطق مفصول عن الشاشة: `validateReturnRequest` و`buildReturnJournal`
//  و`processSalesReturn` دوالٌ نقية تُختبَر بلا متصفح، والشاشة تستدعيها
//  ولا تحسب شيئًا.
//
//  ⚠ والعملية ذرّية: تحديث القطعة وقيد اليومية وحركة النقد إمّا تتمّ
//  كلها أو لا يتمّ شيء. نصفها يعني قطعةً عادت للمخزون بلا قيد يُفسّرها،
//  أو قيدًا لمرتجعٍ لم يعد.
// ═══════════════════════════════════════════════════════════════════════

/// أسباب الإرجاع — تحدّد مصير القطعة.

export { sanitizeText };
